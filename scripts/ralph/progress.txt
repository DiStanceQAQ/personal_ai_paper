## Codebase Patterns
- Python 3.12 with FastAPI + uvicorn for backend, static HTML for frontend
- `uv venv` for virtual environment management, `uv pip install` for dependencies
- `mypy` with `strict = true` in pyproject.toml
- `pytest` with `asyncio_mode = "auto"` and `httpx.ASGITransport` for FastAPI testing
- `Makefile` provides `dev`, `test`, `typecheck`, and `check` targets
- All source files under project root; tests in `tests/` directory
- Static files served from `static/` via FastAPI `StaticFiles` mount
- Use `pip install -e ".[dev]"` for editable install with dev dependencies

## 2026-04-24 18:17 - US-001
- Created project foundation: pyproject.toml, main.py (FastAPI), static/index.html (Web UI), tests/test_main.py
- Health check endpoint at GET /health returns {"status": "healthy", "service": "..."}
- UI entry point at GET / serves index.html with status indicator
- Dev command: `make dev` (uvicorn with reload on port 8000)
- Test command: `make test` (pytest -v), 2 tests pass
- Typecheck: `make typecheck` (mypy strict), passes clean
- **Learnings for future iterations:**
  - Python environment is uv-managed, use `uv venv` + `uv pip install` instead of bare pip
  - httpx.ASGITransport is the recommended way to test FastAPI apps with httpx >=0.28
  - mypy strict mode requires explicit return type annotations on all functions
---

## 2026-04-24 18:19 - US-002
- Created config.py (APP_DATA_DIR, DATABASE_PATH, SPACES_DIR)
- Created db.py with full SQLite schema: spaces, papers, passages, knowledge_cards, notes, app_state
- All space-scoped tables (papers, passages, knowledge_cards, notes) include space_id with FK constraint
- `init_db()` is idempotent (uses CREATE TABLE IF NOT EXISTS)
- get_connection() enables foreign keys and sets row_factory
- 5 tests: table creation, idempotency, FK enforcement, space_id presence, app_state key-value
- **Learnings for future iterations:**
  - Use `tempfile.TemporaryDirectory` for isolated test databases
  - Foreign keys must be enabled per-connection with `PRAGMA foreign_keys = ON`
  - `sqlite3.Row` row_factory allows dict-style column access by name
  - Use Path objects instead of raw strings for database paths
---

## 2026-04-24 18:22 - US-003
- Created routes_spaces.py with FastAPI APIRouter for /api/spaces
- Implemented CRUD: create, list, get, rename, archive, delete
- Active space management: set_active_space and get_active_space via PUT/GET /api/spaces/active
- Space isolation: delete/archive only affects targeted space
- Added database init on app startup via FastAPI lifespan
- 9 API route tests pass
- **Learnings for future iterations:**
  - FastAPI route order matters: fixed routes (like `/active`) MUST be defined before parameterized routes (like `/{space_id}`) or the parameterized route will capture the literal path
  - Pytest fixtures that yield need `Generator[YieldType, None, None]` return annotation
  - Use `@asynccontextmanager` with `AsyncIterator[None]` type for FastAPI lifespan
  - Module-level variable override pattern works for test DB isolation but is fragile — consider dependency injection later
  - `ON CONFLICT(key) DO UPDATE` is the SQLite upsert pattern
---

## 2026-04-24 18:24 - US-004
- Built full space management UI in static/index.html with vanilla JS
- UI lists all spaces with status badges (Active/Archived)
- Create space form with name and description fields
- Inline rename with save/cancel
- Description editing with textarea and save/cancel
- Archive and delete buttons with confirmation dialogs
- "Open" button sets active space; active space highlighted with blue border + "Current" badge
- Active space banner always visible at top
- Changed create_space and update_space to accept JSON Body instead of query params
- **Learnings for future iterations:**
  - FastAPI routes: use `Body(...)` for required fields, `Body("")` for optional defaults
  - httpx test client: use `json={}` for JSON body, `params={}` for query params
  - Vanilla JS SPA pattern: inline edit with display/edit toggle via CSS (display:none)
  - UI uses `esc()` for XSS prevention on user-provided text
  - Browser verification not performed (no browser tools available); manual verification needed
---

## 2026-04-24 18:27 - US-005
- Created routes_papers.py with POST /api/papers/upload and GET /api/papers
- PDF upload saves to app-data/spaces/<space-id>/papers/<uuid>.pdf
- SHA-256 hash computed for every uploaded PDF
- Duplicate detection: same hash within same space → 409 Conflict
- Cross-space duplicates allowed (different spaces can have same PDF)
- Added python-multipart to dependencies for UploadFile support
- UI: drag-and-drop upload zone + file picker, paper list with parse status badges
- Papers section only visible when an active space is set
- 6 new tests pass (upload, non-PDF reject, no-active-space, duplicate detection, cross-space, list)
- **Learnings for future iterations:**
  - FastAPI UploadFile: use `httpx` files param as `{"file": (filename, bytes, mime_type)}` in tests
  - File upload in browser: FormData + fetch with no Content-Type header (browser sets multipart)
  - SHA-256 with incremental reading avoids loading large files entirely in memory
  - Use `hashlib.sha256()` with chunked reading for file hashing
  - Paper list uses `parse_status` badge (pending/parsed/error) styled with CSS classes
---

## 2026-04-24 18:29 - US-006
- Added GET /api/papers/{paper_id} and PATCH /api/papers/{paper_id} endpoints
- PATCH supports all metadata fields: title, authors, year, doi, arxiv_id, pubmed_id, venue, abstract, citation, user_tags, relation_to_idea
- Partial updates: only provided fields are changed, others preserved
- UI: click a paper to open detail/edit panel with form fields for all metadata
- Citation field accepts BibTeX text input
- Relation to idea dropdown with all 8 options
- 4 new tests: get_paper, update all metadata, partial update, 404
- **Learnings for future iterations:**
  - Dynamic SQL UPDATE: build SET clause from non-None fields to support partial updates
  - Paper title fallback display: `esc(p.title || '(Untitled)')` when title is empty
  - Use `parseInt(x) || null` to handle empty year field gracefully
---

## 2026-04-24 18:32 - US-007
- Created parser.py using PyMuPDF for PDF text extraction
- Splits text by page, then section cues, then paragraph boundaries
- Section detection via keyword matching in text (abstract, introduction, method, etc.)
- Passage type mapping: abstract→abstract, method→method, etc.
- POST /api/papers/{paper_id}/parse endpoint triggers parsing
- On failure: paper parse_status set to 'error', paper record preserved
- 6 parser tests, 2 parse endpoint tests, 35 total tests pass
- Added PyMuPDF to dependencies and mypy config with no-untyped-call override
- **Learnings for future iterations:**
  - PyMuPDF lacks type stubs → need `[[tool.mypy.overrides]]` with `disable_error_code`
  - Use `pymupdf.open()` to create test PDFs in memory for unit testing
  - Parse status flow: pending → parsing → parsed (or error)
  - `INSERT OR REPLACE` for passages avoids duplicates on re-parse
  - `finally: doc.close()` critical to avoid resource leaks
---

## 2026-04-24 18:34 - US-008
- Added GET /api/papers/{paper_id}/passages endpoint
- Paper detail UI now shows passages with section badge, type badge, page number, paragraph index
- "Parse PDF" button triggers parsing and refreshes passage display
- Parse status shown in paper detail
- Passage text displayed with pre-wrap for readability
- 2 new tests: list_passages, list_passages_nonexistent_paper (37 total passing)
- **Learnings for future iterations:**
  - Passages ordered by page_number, paragraph_index for sequential reading
  - Use CSS `white-space:pre-wrap` for displaying original text with formatting
  - Passage badges show section (blue) and passage_type (purple) for quick scanning
---

## 2026-04-24 18:38 - US-009
- Standalone FTS5 virtual table on passages (passage_id, paper_id, space_id, section, original_text)
- FTS index created in init_db() alongside schema tables
- Passages auto-inserted into FTS on PDF parsing
- Search API: GET /api/search?q=... with space isolation
- snippet() for highlighted search results with match context
- Search results include paper title, section, page, paragraph index, snippet, original text
- 6 search tests: FTS creation, search results, space isolation, API tests (43 total passing)
- **Learnings for future iterations:**
  - FTS5 content mode (content=tablename) is complex; standalone FTS with manual sync is more reliable
  - Content-mode FTS5 requires content table to have rowid (no WITHOUT ROWID tables)
  - snippet() function works in standalone FTS mode out of the box
  - Use rebuild_fts_index to re-sync from passages table after data changes
  - Space isolation in search: filter by space_id in join condition
---

## 2026-04-24 18:40 - US-010
- Added literature search bar and results area to UI
- Search input with Enter key support
- Results show paper title, section badge, page number, relevance score, and highlighted snippet
- Click a result to open the paper detail view
- Empty state: "No results found for 'query'"
- Error state: graceful message when no active space
- **Learnings for future iterations:**
  - Search results use snippet() with <mark> tags for highlighting in UI
  - encodeURIComponent() needed for search query in URL
  - Results link to paper detail via viewPaper(paper_id) on click
---

## 2026-04-24 18:42 - US-011
- Created routes_cards.py with full CRUD: create, list (with filters), get, update, delete
- 12 card types supported (Problem through Practical Tip)
- Cards bound to paper_id and space_id
- UI: card list in paper detail, create card form with type dropdown, delete button
- update sets user_edited=1 automatically
- 4 card tests pass (47 total)
- **Learnings for future iterations:**
  - Card filtering supports paper_id and card_type query params
  - Card type validation on both create and update
  - Cards displayed with type badge, confidence score, and delete button
---

## 2026-04-24 18:44 - US-012
- Created card_extractor.py with rules-based extraction
- Keyword matching for Method, Result, Limitation, Metric cards
- Every auto-generated card bound to source passage with confidence score
- POST /api/cards/extract/{paper_id} endpoint triggers extraction
- UI: "Auto-Extract" button in paper detail cards section
- Extraction failure doesn't affect search or passage viewing (separate endpoint)
- 6 extractor tests, 53 total tests pass
- **Learnings for future iterations:**
  - Rules-based extraction uses keyword lists for each card type
  - INSERT OR IGNORE prevents duplicate cards on re-extraction
  - _first_sentence() helper truncates passage text for card summaries
  - Card extraction returns count; cards are immediately queryable via GET /api/cards
---

## 2026-04-24 18:45 - US-013
- Added structured literature views: Methods, Metrics, Results, Failure Modes, Limitations, Claims
- Tab-based navigation with active highlighting
- Each view filters cards by type from GET /api/cards?card_type=X
- Cards show type badge, summary, confidence, and link to open paper
- Views section visible when space is active
- **Learnings for future iterations:**
  - Card type tabs use encodeURIComponent for types with spaces (e.g., "Failure Mode")
  - Views reuse existing list_cards API with card_type filter
---

## 2026-04-24 18:50 - US-014 & US-015
- Created mcp_server.py with FastMCP from mcp.server.fastmcp (v1.27.0+ API)
- Core tools: list_spaces, get_active_space, list_papers, search_literature, get_paper_summary, get_citation
- Specialized evidence tools: get_methods, get_metrics, get_limitations, find_failure_modes, find_similar_results, compare_with_literature, get_evidence_for_claim
- Server entry point at `mcp_server:main` via `paper-engine-mcp` console script
- All tools default to active space with optional space_id override
- 9 MCP tests pass (62 total), mypy passes clean on 21 source files
- **Learnings for future iterations:**
  - mcp v1.27.0 uses FastMCP (not Server directly) for @tool() decorator support
  - FastMCP.tool() decorates synchronous functions — no async needed
  - MCP transport: `mcp.run(transport="stdio")` for local agent communication
  - mcp library has no type stubs — needs `ignore_missing_imports` in mypy config
  - _get_cards_by_type helper reduces duplication across 5 card-type tools
  - search_literature and find_similar_results share search_passages backend
  - compare_with_literature enriches search results with related cards from matching papers
  - get_evidence_for_claim combines passage search with Evidence-type card lookup
---

## 2026-04-24 18:55 - US-016
- Created routes_agent.py with agent access control API
  - GET /api/agent/status: returns enabled state, server name, transport, exposed space
  - PUT /api/agent/status: toggle agent access (accepts {enabled: bool})
  - PUT /api/agent/enable, /api/agent/disable: convenience endpoints
- Added _check_access() to mcp_server.py: all 12 MCP tools check agent_access before responding
  - Default: access disabled until user explicitly enables in UI
  - When disabled, all tools return error dict
- UI: added agent access toggle switch in active space banner area
  - Shows MCP transport (stdio), server name, and exposed space name
  - Toggle switch with green/disabled styling
  - loadAgentStatus() called on every space list refresh
- Added 12 new tests in tests/test_agent.py:
  - Agent status default disabled, enable, disable, set_status, shows active space
  - MCP tool blocked when disabled, works when enabled
  - Space isolation for papers, cards, search via MCP tools
  - Source info verification: all agent results include paper_id/passage_id
  - No source-less results: every search result has paper_id and passage_id
- Fixed: duplicate search section in index.html, structured views placement
- 74 tests passing, 23 source files typecheck clean
- **Learnings for future iterations:**
  - FastAPI Body(..., embed=True) needed when body is {key: value} not raw value
  - MCP agent_access stored in app_state table (same pattern as active_space)
  - Default-disabled is safer for agent access (explicit user opt-in)
  - Space isolation verified at 3 levels: papers, knowledge_cards, search passages
  - All MCP results traceable to source (paper_id + passage_id) — no orphan results
  - Use dict[str, Any] return type for dynamic API responses to satisfy strict mypy
---
